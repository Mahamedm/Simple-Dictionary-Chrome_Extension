{
  "manifest_version": 2,
  "name": "Dictionary Extension",
  "description": "Adds a dictionary definition to the context menu when a word is selected.",
  "version": "1.0",

  "permissions": [
    "contextMenus"
  ],

  "background": {
    "scripts": ["background.js"]
  }
}
